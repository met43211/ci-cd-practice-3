services:
  web:
    environment:
      FLASK_APP: app.py
      FLASK_DEBUG: "1"
    volumes:
      - ./flask_app/app.py:/app/app.py:ro
    command:
      [
        "flask",
        "run",
        "--host=0.0.0.0",
        "--port=5000",
        "--reload",
      ]
